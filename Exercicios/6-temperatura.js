const var1 = 1.8;  // Multiplicador para conversão de Celsius para Fahrenheit
const var2 = 32;   // Valor a ser somado para a conversão

let c = 37; // Temperatura em Celsius

const resul = (c * var1) + var2; // Aplica a fórmula de conversão
let f = resul; // Resultado em Fahrenheit

console.log(f); // Exibe a temperatura em Fahrenheit
